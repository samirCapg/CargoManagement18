@{
    ViewBag.Title = "Success";
}
@*@if (Session["UserName"] != null)
    {
        <p>Welcome, @Session["UserName"]!</p>
        <a href="@Url.Action("Logout", "CargoApp")" class="w-1/2 bg-gray-600 text-white py-2 rounded-md text-xl text-center hover:bg-gray-700 transition duration-300">Logout</a>

    }
    else
    {
        <p>Please log in.</p>
}*@



<div class="flex items-center justify-center bg-gray-100 p-12">
    <div class="bg-white p-6 rounded-lg shadow-lg w-full max-w-md text-center">
        <h2 class="text-2xl text-green-600 font-semibold mb-4">Success</h2>
        <p class="text-gray-700 mb-4">@TempData["Message"]</p>

        <!-- Back Link -->
        @if (Request.UrlReferrer != null)
        {
            <a href="@Request.UrlReferrer.AbsoluteUri"
               class="inline-block px-6 py-2 mt-4 bg-blue-500 text-white rounded-lg hover:bg-blue-600 transition duration-300">
                Go Back
            </a>
        }
    </div>
</div>
